// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Manage a collection of alerts as a single resource. The resource batches create/update/delete calls and automatically chunks requests that exceed the API's per-request limits (30 for create/update, 20 for delete).
 *
 * > **Note** Do not manage the same alert with both `astronomer.Alert` and `astronomer.Alerts`. Each resource claims ownership of the alerts it manages, so overlapping definitions conflict and cause churn on every apply. Use one resource or the other for a given alert.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as astronomer from "@ryan-pip/pulumi_astronomer";
 * import * as std from "@pulumi/std";
 *
 * // The astro_alerts resource manages many alerts as a single resource. It batches the underlying
 * // API calls and automatically chunks requests that exceed the per-request limits (30 for
 * // create/update, 20 for delete), so you can declare any number of alerts in one block.
 * //
 * // Alerts are keyed by a stable, user-defined string. Changing a key is treated as deleting the old
 * // alert and creating a new one.
 * const teamAlerts = new astronomer.Alerts("team_alerts", {alerts: {
 *     etl_dag_failure: {
 *         name: "ETL DAG Failure",
 *         type: "DAG_FAILURE",
 *         severity: "CRITICAL",
 *         entityType: "DEPLOYMENT",
 *         entityId: "cm1zkps2a0cv301ph39benet6",
 *         notificationChannelIds: ["cm4nwrvyg024h01mk2dn58m5s"],
 *         rules: {
 *             properties: {
 *                 deploymentId: "cm1zkps2a0cv301ph39benet6",
 *             },
 *             patternMatches: [{
 *                 entityType: "DAG_ID",
 *                 operatorType: "IS",
 *                 values: ["etl_dag"],
 *             }],
 *         },
 *     },
 *     reporting_dag_duration: {
 *         name: "Reporting DAG Duration",
 *         type: "DAG_DURATION",
 *         severity: "WARNING",
 *         entityType: "DEPLOYMENT",
 *         entityId: "cm1zkps2a0cv301ph39benet6",
 *         notificationChannelIds: ["cm4nwrvyg024h01mk2dn58m5s"],
 *         rules: {
 *             properties: {
 *                 deploymentId: "cm1zkps2a0cv301ph39benet6",
 *                 dagDurationSeconds: 3600,
 *             },
 *             patternMatches: [{
 *                 entityType: "DAG_ID",
 *                 operatorType: "IS",
 *                 values: ["reporting_dag"],
 *             }],
 *         },
 *     },
 * }});
 * // Generate many alerts programmatically — the resource chunks them across requests for you.
 * const perDagFailureAlerts = new astronomer.Alerts("per_dag_failure_alerts", {alerts: std.toset({
 *     input: [
 *         "a",
 *         "b",
 *         "c",
 *     ],
 * }).then(invoke => .reduce((__obj, dag) => ({ ...__obj, [`${dag}_failure`]: {
 *     name: `${dag} DAG Failure`,
 *     type: "DAG_FAILURE",
 *     severity: "CRITICAL",
 *     entityType: "DEPLOYMENT",
 *     entityId: "cm1zkps2a0cv301ph39benet6",
 *     notificationChannelIds: ["cm4nwrvyg024h01mk2dn58m5s"],
 *     rules: {
 *         properties: {
 *             deploymentId: "cm1zkps2a0cv301ph39benet6",
 *         },
 *         patternMatches: [{
 *             entityType: "DAG_ID",
 *             operatorType: "IS",
 *             values: [dag],
 *         }],
 *     },
 * } }), {}))});
 * ```
 */
export class Alerts extends pulumi.CustomResource {
    /**
     * Get an existing Alerts resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AlertsState, opts?: pulumi.CustomResourceOptions): Alerts {
        return new Alerts(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'astronomer:index/alerts:Alerts';

    /**
     * Returns true if the given object is an instance of Alerts.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Alerts {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Alerts.__pulumiType;
    }

    /**
     * A map of alerts to manage as a single resource, keyed by a stable user-defined string.
     */
    declare public readonly alerts: pulumi.Output<{[key: string]: outputs.AlertsAlerts}>;

    /**
     * Create a Alerts resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AlertsArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AlertsArgs | AlertsState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AlertsState | undefined;
            resourceInputs["alerts"] = state?.alerts;
        } else {
            const args = argsOrState as AlertsArgs | undefined;
            if (args?.alerts === undefined && !opts.urn) {
                throw new Error("Missing required property 'alerts'");
            }
            resourceInputs["alerts"] = args?.alerts;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Alerts.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Alerts resources.
 */
export interface AlertsState {
    /**
     * A map of alerts to manage as a single resource, keyed by a stable user-defined string.
     */
    alerts?: pulumi.Input<{[key: string]: pulumi.Input<inputs.AlertsAlerts>} | undefined>;
}

/**
 * The set of arguments for constructing a Alerts resource.
 */
export interface AlertsArgs {
    /**
     * A map of alerts to manage as a single resource, keyed by a stable user-defined string.
     */
    alerts: pulumi.Input<{[key: string]: pulumi.Input<inputs.AlertsAlerts>}>;
}
