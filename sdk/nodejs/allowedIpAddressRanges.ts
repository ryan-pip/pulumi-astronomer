// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Manage an organization's IP access list as a single resource. This resource is authoritative: any allowed IP address ranges not present in `ipAddressRanges` are removed on apply.
 *
 * > **Warning: risk of lockout.** While the access list is empty, access is unrestricted. Adding the first range turns enforcement on, and the API will reject that first apply with a `400` unless the submitted ranges include the public IP address of the machine running Terraform (for example, a CI runner's egress IP). Once enforcement is on, the API does **not** protect you from removing the range that covers your own IP: doing so - or narrowing/replacing it so your IP is no longer covered - locks you (and this provider) out and will fail the apply mid-way. Always keep a range covering the machine that runs Terraform.
 *
 * > **Note** Do not manage the IP access list with more than one `astronomer.AllowedIpAddressRanges` resource. To adopt an access list that already exists, import it first (see below) rather than re-declaring it, otherwise the first apply will conflict with the existing ranges.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as astronomer from "@ryan-pip/pulumi_astronomer";
 *
 * // The astro_allowed_ip_address_ranges resource manages an organization's full IP access list as a
 * // single resource. It is authoritative: any ranges not listed here are removed on apply.
 * //
 * // The resource batches the underlying create/delete calls and automatically chunks requests that
 * // exceed the API's per-request limit (1000 CIDRs).
 * const orgAllowList = new astronomer.AllowedIpAddressRanges("org_allow_list", {ipAddressRanges: [
 *     "203.0.113.0/24",
 *     "198.51.100.5/32",
 * ]});
 * ```
 *
 * ## Import
 *
 * The IP access list is a singleton per organization. Import it using the organization ID; the
 * provider reads the current set of ranges into ip_address_ranges. Import (rather than re-declaring)
 * to adopt an access list that already exists, otherwise the first apply conflicts with the existing
 * ranges.
 *
 * ```sh
 * $ pulumi import astronomer:index/allowedIpAddressRanges:AllowedIpAddressRanges org_allow_list <organization_id>
 * ```
 */
export class AllowedIpAddressRanges extends pulumi.CustomResource {
    /**
     * Get an existing AllowedIpAddressRanges resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AllowedIpAddressRangesState, opts?: pulumi.CustomResourceOptions): AllowedIpAddressRanges {
        return new AllowedIpAddressRanges(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'astronomer:index/allowedIpAddressRanges:AllowedIpAddressRanges';

    /**
     * Returns true if the given object is an instance of AllowedIpAddressRanges.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AllowedIpAddressRanges {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AllowedIpAddressRanges.__pulumiType;
    }

    /**
     * The organization's allowed IP address ranges, in CIDR format (e.g. `203.0.113.0/24`). This resource authoritatively manages the organization's full IP access list - ranges not included here are removed on apply. An empty set removes all restrictions.
     */
    declare public readonly ipAddressRanges: pulumi.Output<string[]>;

    /**
     * Create a AllowedIpAddressRanges resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AllowedIpAddressRangesArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AllowedIpAddressRangesArgs | AllowedIpAddressRangesState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AllowedIpAddressRangesState | undefined;
            resourceInputs["ipAddressRanges"] = state?.ipAddressRanges;
        } else {
            const args = argsOrState as AllowedIpAddressRangesArgs | undefined;
            if (args?.ipAddressRanges === undefined && !opts.urn) {
                throw new Error("Missing required property 'ipAddressRanges'");
            }
            resourceInputs["ipAddressRanges"] = args?.ipAddressRanges;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AllowedIpAddressRanges.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AllowedIpAddressRanges resources.
 */
export interface AllowedIpAddressRangesState {
    /**
     * The organization's allowed IP address ranges, in CIDR format (e.g. `203.0.113.0/24`). This resource authoritatively manages the organization's full IP access list - ranges not included here are removed on apply. An empty set removes all restrictions.
     */
    ipAddressRanges?: pulumi.Input<pulumi.Input<string>[] | undefined>;
}

/**
 * The set of arguments for constructing a AllowedIpAddressRanges resource.
 */
export interface AllowedIpAddressRangesArgs {
    /**
     * The organization's allowed IP address ranges, in CIDR format (e.g. `203.0.113.0/24`). This resource authoritatively manages the organization's full IP access list - ranges not included here are removed on apply. An empty set removes all restrictions.
     */
    ipAddressRanges: pulumi.Input<pulumi.Input<string>[]>;
}
