# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities

__all__ = ['AllowedIpAddressRangesArgs', 'AllowedIpAddressRanges']

@pulumi.input_type
class AllowedIpAddressRangesArgs:
    def __init__(__self__, *,
                 ip_address_ranges: pulumi.Input[Sequence[pulumi.Input[_builtins.str]]]):
        """
        The set of arguments for constructing a AllowedIpAddressRanges resource.

        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] ip_address_ranges: The organization's allowed IP address ranges, in CIDR format (e.g. `203.0.113.0/24`). This resource authoritatively manages the organization's full IP access list - ranges not included here are removed on apply. An empty set removes all restrictions.
        """
        pulumi.set(__self__, "ip_address_ranges", ip_address_ranges)

    @_builtins.property
    @pulumi.getter(name="ipAddressRanges")
    def ip_address_ranges(self) -> pulumi.Input[Sequence[pulumi.Input[_builtins.str]]]:
        """
        The organization's allowed IP address ranges, in CIDR format (e.g. `203.0.113.0/24`). This resource authoritatively manages the organization's full IP access list - ranges not included here are removed on apply. An empty set removes all restrictions.
        """
        return pulumi.get(self, "ip_address_ranges")

    @ip_address_ranges.setter
    def ip_address_ranges(self, value: pulumi.Input[Sequence[pulumi.Input[_builtins.str]]]):
        pulumi.set(self, "ip_address_ranges", value)


@pulumi.input_type
class _AllowedIpAddressRangesState:
    def __init__(__self__, *,
                 ip_address_ranges: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None):
        """
        Input properties used for looking up and filtering AllowedIpAddressRanges resources.

        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] ip_address_ranges: The organization's allowed IP address ranges, in CIDR format (e.g. `203.0.113.0/24`). This resource authoritatively manages the organization's full IP access list - ranges not included here are removed on apply. An empty set removes all restrictions.
        """
        if ip_address_ranges is not None:
            pulumi.set(__self__, "ip_address_ranges", ip_address_ranges)

    @_builtins.property
    @pulumi.getter(name="ipAddressRanges")
    def ip_address_ranges(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        The organization's allowed IP address ranges, in CIDR format (e.g. `203.0.113.0/24`). This resource authoritatively manages the organization's full IP access list - ranges not included here are removed on apply. An empty set removes all restrictions.
        """
        return pulumi.get(self, "ip_address_ranges")

    @ip_address_ranges.setter
    def ip_address_ranges(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "ip_address_ranges", value)


@pulumi.type_token("astronomer:index/allowedIpAddressRanges:AllowedIpAddressRanges")
class AllowedIpAddressRanges(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 ip_address_ranges: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 __props__=None):
        """
        Manage an organization's IP access list as a single resource. This resource is authoritative: any allowed IP address ranges not present in `ip_address_ranges` are removed on apply.

        > **Warning: risk of lockout.** While the access list is empty, access is unrestricted. Adding the first range turns enforcement on, and the API will reject that first apply with a `400` unless the submitted ranges include the public IP address of the machine running Terraform (for example, a CI runner's egress IP). Once enforcement is on, the API does **not** protect you from removing the range that covers your own IP: doing so - or narrowing/replacing it so your IP is no longer covered - locks you (and this provider) out and will fail the apply mid-way. Always keep a range covering the machine that runs Terraform.

        > **Note** Do not manage the IP access list with more than one `AllowedIpAddressRanges` resource. To adopt an access list that already exists, import it first (see below) rather than re-declaring it, otherwise the first apply will conflict with the existing ranges.

        ## Example Usage

        ```python
        import pulumi
        import pulumi_astronomer as astronomer

        # The astro_allowed_ip_address_ranges resource manages an organization's full IP access list as a
        # single resource. It is authoritative: any ranges not listed here are removed on apply.
        #
        # The resource batches the underlying create/delete calls and automatically chunks requests that
        # exceed the API's per-request limit (1000 CIDRs).
        org_allow_list = astronomer.AllowedIpAddressRanges("org_allow_list", ip_address_ranges=[
            "203.0.113.0/24",
            "198.51.100.5/32",
        ])
        ```

        ## Import

        The IP access list is a singleton per organization. Import it using the organization ID; the
        provider reads the current set of ranges into ip_address_ranges. Import (rather than re-declaring)
        to adopt an access list that already exists, otherwise the first apply conflicts with the existing
        ranges.

        ```sh
        $ pulumi import astronomer:index/allowedIpAddressRanges:AllowedIpAddressRanges org_allow_list <organization_id>
        ```


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] ip_address_ranges: The organization's allowed IP address ranges, in CIDR format (e.g. `203.0.113.0/24`). This resource authoritatively manages the organization's full IP access list - ranges not included here are removed on apply. An empty set removes all restrictions.
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: AllowedIpAddressRangesArgs,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        Manage an organization's IP access list as a single resource. This resource is authoritative: any allowed IP address ranges not present in `ip_address_ranges` are removed on apply.

        > **Warning: risk of lockout.** While the access list is empty, access is unrestricted. Adding the first range turns enforcement on, and the API will reject that first apply with a `400` unless the submitted ranges include the public IP address of the machine running Terraform (for example, a CI runner's egress IP). Once enforcement is on, the API does **not** protect you from removing the range that covers your own IP: doing so - or narrowing/replacing it so your IP is no longer covered - locks you (and this provider) out and will fail the apply mid-way. Always keep a range covering the machine that runs Terraform.

        > **Note** Do not manage the IP access list with more than one `AllowedIpAddressRanges` resource. To adopt an access list that already exists, import it first (see below) rather than re-declaring it, otherwise the first apply will conflict with the existing ranges.

        ## Example Usage

        ```python
        import pulumi
        import pulumi_astronomer as astronomer

        # The astro_allowed_ip_address_ranges resource manages an organization's full IP access list as a
        # single resource. It is authoritative: any ranges not listed here are removed on apply.
        #
        # The resource batches the underlying create/delete calls and automatically chunks requests that
        # exceed the API's per-request limit (1000 CIDRs).
        org_allow_list = astronomer.AllowedIpAddressRanges("org_allow_list", ip_address_ranges=[
            "203.0.113.0/24",
            "198.51.100.5/32",
        ])
        ```

        ## Import

        The IP access list is a singleton per organization. Import it using the organization ID; the
        provider reads the current set of ranges into ip_address_ranges. Import (rather than re-declaring)
        to adopt an access list that already exists, otherwise the first apply conflicts with the existing
        ranges.

        ```sh
        $ pulumi import astronomer:index/allowedIpAddressRanges:AllowedIpAddressRanges org_allow_list <organization_id>
        ```


        :param str resource_name: The name of the resource.
        :param AllowedIpAddressRangesArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(AllowedIpAddressRangesArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 ip_address_ranges: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = AllowedIpAddressRangesArgs.__new__(AllowedIpAddressRangesArgs)

            if ip_address_ranges is None and not opts.urn:
                raise TypeError("Missing required property 'ip_address_ranges'")
            __props__.__dict__["ip_address_ranges"] = ip_address_ranges
        super(AllowedIpAddressRanges, __self__).__init__(
            'astronomer:index/allowedIpAddressRanges:AllowedIpAddressRanges',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            ip_address_ranges: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None) -> 'AllowedIpAddressRanges':
        """
        Get an existing AllowedIpAddressRanges resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] ip_address_ranges: The organization's allowed IP address ranges, in CIDR format (e.g. `203.0.113.0/24`). This resource authoritatively manages the organization's full IP access list - ranges not included here are removed on apply. An empty set removes all restrictions.
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _AllowedIpAddressRangesState.__new__(_AllowedIpAddressRangesState)

        __props__.__dict__["ip_address_ranges"] = ip_address_ranges
        return AllowedIpAddressRanges(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter(name="ipAddressRanges")
    def ip_address_ranges(self) -> pulumi.Output[Sequence[_builtins.str]]:
        """
        The organization's allowed IP address ranges, in CIDR format (e.g. `203.0.113.0/24`). This resource authoritatively manages the organization's full IP access list - ranges not included here are removed on apply. An empty set removes all restrictions.
        """
        return pulumi.get(self, "ip_address_ranges")

